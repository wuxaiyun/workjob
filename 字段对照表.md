# 设备台账系统 · 全字段对照表

> 用途：把**旧资料**整理成下表格式后，通过系统「设备批量导入」页或业务页面录入到新系统。
> 数据存储：Cloudflare D1 本地库 `equipment-ledger-db`（worker 用 `env.DB`），照片/附件在 R2。

---

## 一、总体说明

| # | 数据对象 | 存放表 | 录入方式 |
|---|----------|--------|----------|
| 1 | 设备台账 | `equipment` | **Excel 批量导入**（唯一导入入口）或设备表单 |
| 2 | 维修记录 | `repair` | 维修单表单（自动生成单号） |
| 3 | 检定/强检 | `calibration` | 检定管理页 |
| 4 | 备件更换 | `part_replace` | 维修详情·备件栏 |
| 5 | 费用工时 | `cost` | 维修详情·费用栏 |
| 6 | 照片 | `photo` | 仅文件上传（R2，不支持表格数据） |
| 7 | 附件 | `attachment` | 仅文件上传（R2，不支持表格数据） |
| 8 | 字典项 | `dict` | 「基础选项设置」页面 |
| 9 | 分类专属字段 | `field_config` + `equipment.extra_data` | 「设备参数设置」配置 + 设备表单 |
| 10 | 用户 | `users` | 系统初始化脚本提供 admin |

**界面入口**：登录后左侧菜单「设备台账 / 设备批量导入 / 设备参数设置 / 基础选项设置」等。

---

## 二、设备主表 `equipment`（Excel 批量导入）

> 导入页：**设备批量导入**。只认下表**中文表头**；可缺列；每行一台设备；`位号` 全表唯一。

| 中文表头 | 字段名 | 类型 | 必填 | 允许值 / 格式 | 说明 |
|----------|--------|------|------|----------------|------|
| 位号 | `tag_no` | 文本 | ✔ | 唯一 | 文件内不可重复；`insert` 模式重复则跳过该行 |
| 设备名称 | `name` | 文本 | ✔ | | |
| 设备类别 | `category` | 文本 | ✔ | 阀门 / 仪表 / 电机 / 泵/风机 / 锅炉/压力容器 / 其他 | 必须与字典完全一致 |
| 所属项目 | `project` | 文本 | | | 建议与「部门」字典项一致 |
| 项目编号 | `project_no` | 文本 | | | |
| 所属部门 | `department` | 文本 | | | |
| 安装位置 | `location` | 文本 | | | |
| 规格型号 | `model` | 文本 | | | |
| 生产厂家 | `manufacturer` | 文本 | | | |
| 供应商 | `supplier` | 文本 | | | |
| 出厂日期 | `factory_date` | 日期 | | `YYYY-MM-DD` | 空单元格 / 非法日期判为不合规行 |
| 出厂编号 | `factory_no` | 文本 | | | |
| 投用日期 | `commission_date` | 日期 | | `YYYY-MM-DD` | |
| 资产编号 | `asset_no` | 文本 | | | |
| 原值 | `original_value` | 数值 | | 数字 | 文本数字会被转换，非数字判为不合规 |
| 净值 | `net_value` | 数值 | | 数字 | |
| 是否强检 | `is_mandatory` | 枚举 | | 是 / 否 | 建议「是」，配合强检有效期 |
| 强检有效期 | `verify_valid_until` | 日期 | | `YYYY-MM-DD` | |
| 状态 | `status` | 枚举 | | 在用 / 停用 / 报废 | 缺省写入默认值「在用」 |
| 备注 | `remark` | 文本 | | | |

### 校验与导入规则（importExport.ts 固定逻辑）
1. 必填：`位号`、`设备名称`、`设备类别`；位号空 / 文件内重复 → 该行跳过并报错。
2. 日期列（出厂日期/投用日期/强检有效期）必须是 `YYYY-MM-DD`。
3. 数值列（原值/净值）必须是数字。
4. 模式：
   - `insert` 仅新增：位号已存在 → 该行跳过；
   - `upsert` 新增或更新：按位号覆盖更新已存在行（更新 `updated_at/updated_by/version` 并加字段）。
5. 单次最多 5000 行。
6. 先点「预演校验」（不落库），确认无红错后再「正式导入」。
7. 支持 `.xlsx .xls .csv`；CSV 请用 UTF-8 编码以免中文乱码。

> 注意：**导出**的 Excel 表头是英文字段名（含分类字段展开列），**导入**只认中文表头；如需回导，把表头改回中文。

### 分类专属字段列（支持导入，写入 `equipment.extra_data`）

模板会额外包含第九节列出的**全部类别字段**（中文标签即列头：介质、密度、粘度、口径、量程、材质、防护等级、功率……）。规则：
1. 表头 = 中文标签（与「设备参数设置」一致）；每行只填该设备类别相关的列，其余留空。
2. 多个类别共用的标签（如 介质、功率、防护等级）在同一文件里只会出现一次。
3. 后端按「标签 → 字段key」映射写入 `extra_data`（JSON）；未知列会被忽略，不影响导入。
4. `upsert` 更新时会与已存在记录的分类字段**合并**（旧值保留、新值覆盖），不会清空。

---

## 三、维修记录 `repair`

> 录入：维修单表单（自动带出设备快照）。如需旧记录批量进入，可在数据库层按此结构写入（`repair_no` 有唯一约束，系统格式 `RF-YYYYMMDD-001`，建议沿用以免冲突）。

| 字段名 | 类型 | 必填 | 允许值 / 格式 | 说明 |
|--------|------|------|----------------|------|
| `repair_no` | 文本 | 系统生成 | `RF-YYYYMMDD-001` | 唯一，勿手工编重 |
| `tag_no` | 文本 | ✔ | 设备位号 | 须已在 equipment 存在 |
| `equipment_name_snapshot` | 文本 | 自动 | | 设备名称快照 |
| `project_snapshot` | 文本 | 自动 | | 项目快照 |
| `department_snapshot` | 文本 | 自动 | | 部门快照 |
| `location_snapshot` | 文本 | 自动 | | 位置快照 |
| `report_date` | 日期 | ✔ | `YYYY-MM-DD` | 报修日期 |
| `repair_date` | 日期 | | `YYYY-MM-DD` | 维修日期 |
| `repair_type` | 文本 | ✔ | 字典「维修类别」 | 故障维修 / 预防性维护 / 定期检修 / 紧急抢修 / 其他 |
| `status` | 文本 | ✔ | 字典「维修状态」 | 待处理 / 处理中 / 待验收 / 已完成 / 已关闭 / 取消 |
| `worker` | 文本 | | | 维修人，默认当前登录人 |
| `fault_desc` | 文本 | ✔ | | 故障描述 |
| `cause` | 文本 | | | 原因分析 |
| `repair_content` | 文本 | ✔ | | 维修内容 |
| `action` | 文本 | | | 处理措施 |
| `remark` | 文本 | | | |
| `version` | 整数 | 系统 | 默认 1 | 乐观锁 |

---

## 四、检定 / 强检 `calibration`

> 录入：**检定管理**页。强检有效期在设备详情与首页「强检到期」提示中用到的即 `due_date`。

| 字段名 | 类型 | 必填 | 允许值 / 格式 | 说明 |
|--------|------|------|----------------|------|
| `tag_no` | 文本 | ✔ | 设备位号 | |
| `calibration_no` | 文本 | | | 检定（证书/委托）编号 |
| `cert_no` | 文本 | | | 证书编号 |
| `done_date` | 日期 | | `YYYY-MM-DD` | 本次检定日期 |
| `due_date` | 日期 | ✔ | `YYYY-MM-DD` | 下次检定（到期）日期，首页临期提醒依据 |
| `result` | 文本 | | 字典「检定结果」 | 合格 / 不合格 / 待检定 |
| `remark` | 文本 | | | |
| `created_at / updated_at / created_by` | 系统 | | | 自动 |

---

## 五、备件更换 `part_replace`

| 字段名 | 类型 | 必填 | 允许值 / 格式 | 说明 |
|--------|------|------|----------------|------|
| `repair_no` | 文本 | | 维修单号 | 关联维修单（可空） |
| `tag_no` | 文本 | ✔ | 设备位号 | |
| `part_name` | 文本 | ✔ | | 备件名称 |
| `part_model` | 文本 | | | 备件型号 |
| `part_qty` | 数值 | | 默认 1 | 数量 |
| `part_unit` | 文本 | | 字典「备件单位」 | 个 / 台 / 只 / 套 / 根 / 张 |
| `replace_date` | 日期 | | `YYYY-MM-DD` | 更换日期 |
| `cost` | 数值 | | | 费用 |
| `remark` | 文本 | | | |
| `created_at / created_by` | 系统 | | | 自动 |

---

## 六、费用工时 `cost`

| 字段名 | 类型 | 必填 | 允许值 / 格式 | 说明 |
|--------|------|------|----------------|------|
| `repair_no` | 文本 | | 维修单号 | 关联维修单（可空） |
| `tag_no` | 文本 | ✔ | 设备位号 | |
| `cost_type` | 文本 | ✔ | 字典「费用类型」 | 维修费 / 工时费 / 备件费 / 材料费 / 其他 |
| `amount` | 数值 | ✔ | | 金额 |
| `hours` | 数值 | | | 工时 |
| `cost_date` | 日期 | | `YYYY-MM-DD` | |
| `remark` | 文本 | | | |
| `created_at / created_by` | 系统 | | | 自动 |

---

## 七、照片 / 附件（不支持表格导入）

| 表 | 字段名 | 说明 |
|----|--------|------|
| `photo` | `tag_no` `repair_no` `photo_type` `file_name` `object_key` `thumbnail_key` `description` `photographer` `photo_time` `upload_date` `remark` | `photo_type` 用字典「照片类型」：设备现场 / 维修前 / 维修后 / 故障部位 / 铭牌 / 备件 / 其他；`object_key`/`thumbnail_key` 由上传接口生成 R2 路径，勿手工填 |
| `attachment` | `tag_no` `repair_no` `file_type` `file_name` `object_key` `upload_date` `uploader` `description` `remark` | `file_type` 用字典「附件类型」：说明书 / 图纸 / P&ID / 合格证 / 检定证书 / 其他 |

> 旧照片/附件请使用系统「照片 / 附件」上传功能（或提前放入本机对应位号目录后按位号上传）。**照片与附件在数据库中只保存元数据，真实文件必须进 R2**。

---

## 八、字典表 `dict`（「基础选项设置」页面维护）

> 系统所有下拉选项都来自此表，改值/新增项立即生效。`sort_order` 控制排序，数字小的在前，末位项可用 99。

| 字典类型 | 取值（顺序） |
|----------|--------------|
| 设备类别 | 阀门(1)、仪表(2)、电机(3)、泵/风机(4)、锅炉/压力容器(5)、其他(99) |
| 维修类别 | 故障维修(1)、预防性维护(2)、定期检修(3)、紧急抢修(4)、其他(99) |
| 维修状态 | 待处理(1)、处理中(2)、待验收(3)、已完成(4)、已关闭(5)、取消(6) |
| 设备状态 | 在用(1)、停用(2)、报废(3) |
| 照片类型 | 设备现场(1)、维修前(2)、维修后(3)、故障部位(4)、铭牌(5)、备件(6)、其他(99) |
| 附件类型 | 说明书(1)、图纸(2)、P&ID(3)、合格证(4)、检定证书(5)、其他(99) |
| 部门 | 一期项目(1)、二期项目(2)、三期项目(3)、公用工程部(4)、设备部(5) |
| 费用类型 | 维修费(1)、工时费(2)、备件费(3)、材料费(4)、其他(99) |
| 备件单位 | 个(1)、台(2)、只(3)、套(4)、根(5)、张(6) |
| 检定结果 | 合格(1)、不合格(2)、待检定(3) |
| 导入模式 | insert(1)、upsert(2)、update(3)（系统内部标识） |

---

## 九、分类专属字段 `field_config` → `equipment.extra_data`

> 「设备参数设置」页按类别配置；录入时这些字段填在设备表单的分组里，存在 `equipment.extra_data`（JSON）。**Excel 批量导入已支持这些列**：模板表头 = 中文标签，后端映射到对应 field_key 写入 `extra_data`（见第二节·分类专属字段列小节）。

### 通用工艺字段（全部类别）
| 中文字段 | field_key | |
|----------|-----------|---|
| 介质 | `medium` | |
| 密度 | `density` | |
| 粘度 | `viscosity` | |

### 阀门
| 分组 | 中文字段 | field_key |
|------|----------|-----------|
| 基本参数 | 口径 | `caliber` |
| 基本参数 | 量程 | `range` |
| 基本参数 | 填料 | `packing` |
| 基本参数 | 泄漏等级 | `leakage_class` |
| 基本参数 | 流量特性 | `flow_char` |
| 基本参数 | 压力等级 | `pressure_class` |
| 基本参数 | 执行机构型号 | `actuator_model` |
| 材质 | 阀体材质 | `body_material` |
| 材质 | 阀芯材质 | `core_material` |
| 材质 | 阀座材质 | `seat_material` |
| 安装 | 连接形式 | `connection_type` |
| 安装 | 法兰标准 | `flange_std` |
| 安装 | 法兰尺寸 | `flange_size` |

### 仪表
| 分组 | 中文字段 | field_key |
|------|----------|-----------|
| 基本参数 | 量程 | `range` |
| 基本参数 | 准确度 | `accuracy` |
| 基本参数 | 分度值 | `min_display` |
| 环境 | 防护等级 | `protection_level` |
| 环境 | 防爆等级 | `explosion_proof` |
| 安装 | 电气接口尺寸 | `elec_interface` |

### 电机
| 分组 | 中文字段 | field_key |
|------|----------|-----------|
| 基本参数 | 功率 | `power` |
| 基本参数 | 电机型号 | `motor_model` |
| 环境 | 防护等级 | `protection_level` |
| 环境 | 防爆等级 | `explosion_proof` |
| 关键部件 | 轴承 | `bearing` |
| 关键部件 | 减速机 | `gearbox` |

### 泵/风机
| 分组 | 中文字段 | field_key |
|------|----------|-----------|
| 基本参数 | 功率 | `power` |
| 运行参数 | 最大流量 | `flow_max` |
| 运行参数 | 正常流量 | `flow_normal` |
| 运行参数 | 最小流量 | `flow_min` |
| 运行参数 | 操作压力 | `op_pressure` |
| 运行参数 | 操作温度 | `op_temperature` |
| 关键部件 | 轴承 | `bearing` |
| 关键部件 | 减速机 | `gearbox` |
| 关键部件 | 密封件 | `seal` |

### 锅炉/压力容器
| 分组 | 中文字段 | field_key |
|------|----------|-----------|
| 基本参数 | 压力等级 | `pressure_class` |
| 基本参数 | 温度 | `temperature` |
| 材质 | 材质 | `material` |
| 运行参数 | 阀前压力 | `pressure_valve_before` |
| 运行参数 | 阀后压力 | `pressure_valve_after` |
| 运行参数 | 压差 | `pressure_diff` |

`field_config` 其余字段：`category`(类别)、`field_label`(中文名)、`group_name`(分组名)、`sort_order`(序号)、`is_required/is_visible_list/is_visible_detail/is_visible_export`(开关 0/1)、`default_value`(默认值)。

---

## 十、用户 `users`

| 字段名 | 说明 |
|--------|------|
| `username` | 登录名，唯一 |
| `password_hash` | 密码 PBKDF2 加密串，**不导出、不写明文** |
| `role` | `admin`（管理员）/ `worker`（维修工） |
| `real_name` | 真实姓名 |
| `status` | 默认 `active` |
| `created_at` | 自动 |

初始账号：`admin / admin123`。

---

## 十一、系统自动字段（无需填写）

所有业务表都含：`id`(自增主键)、`created_at`、`updated_at`、`created_by`、`updated_by`、`version`(乐观锁)。
`equipment` 另含软删除字段 `deleted_at`、`deleted_by`；日志表 `operation_log`、`import_log`、`backup_log` 由系统自动写入，勿手工维护。

---

## 十二、旧数据迁移建议（推荐流程）

1. 把旧资料整理为 Excel，列头用**第二节的中文表头**，一行一台设备，`位号` 全局唯一。
2. 枚举列（设备类别/状态/是否强检）严格用本表给定值，避免被当作不合规行跳过。
3. 打开系统「设备批量导入」→ 上传 → 先「预演校验」→ 无误后「正式导入」（首次用 `insert`，后续更新用 `upsert`）。
4. 分类专属参数（口径、量程、介质等）直接在**同一份模板**里填写对应类别列即可，一次导入完成，无需事后逐条补充。
5. 检定记录/备件/费用：旧数据较多时建议在检定管理页与维修详情中补充录入；需要大表批量写入时，可按第四节起列的字段结构直接在 D1 中 INSERT（`tag_no` 须已存在，`repair_no` 勿与系统格式冲突）。
6. 照片/附件走系统上传界面，确保 R2 中保存真实文件。